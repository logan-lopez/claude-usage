{
  "name": "claude-usage",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "bin": {
    "cusage": "src/cli.ts",
    "cusage-tui": "src/tui.ts"
  },
  "scripts": {
    "sync": "bun run src/cli.ts sync",
    "fixture": "bun run tools/make-fixture.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "commander": "15.0.0"
  }
}
